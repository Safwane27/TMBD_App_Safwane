<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lenovo.tmbd_safwane">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="TMBD_APP_Safwane"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".MovieListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MovieGridActivity" ></activity>
        <activity android:name=".MovieActivity" ></activity>
        <activity android:name=".SerieListActivity" ></activity>
        <activity android:name=".FavouriteListActivity" ></activity>
        <activity android:name=".SerieGridActivity" ></activity>
        <activity android:name=".SerieActivity" ></activity>
    </application>

</manifest>